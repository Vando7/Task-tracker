# This machine's uplink drops connections mid-body. Be patient and retry hard
# rather than failing an otherwise-fine install.
fetch-retries=10
fetch-retry-mintimeout=5000
fetch-retry-maxtimeout=120000
fetch-timeout=600000
network-concurrency=4
