version: '3'

services:
  docs:
    image: tracker_local_docs
    container_name: tracker_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./tracker:/app/tracker:z
    ports:
      - '9000:9000'
    command: /start-docs
